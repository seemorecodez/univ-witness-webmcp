name: UNIV Deploy required gate

on:
  push:
    branches: ['main']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: univ-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  univ-deploy-required-gate:
    name: UNIV Deploy required gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.13.0'
          cache: npm
      - name: Install pinned dependencies
        run: npm ci
      - name: Install WASI Preview 2 target
        run: rustup target add wasm32-wasip2
      - name: Test portable workload policy
        run: cargo test --manifest-path diagnostic/Cargo.toml
      - name: Compile portable workload source
        run: npm run build:diagnostic
      - name: Reproduce browser modules from pinned component
        run: >-
          npx --yes --package @bytecodealliance/jco@1.17.9 jco transpile
          diagnostic/component/univ-portable-workload.wasm
          -o "$RUNNER_TEMP/univ-deploy-generated"
          --name univ-portable-workload
          --instantiation async
          --no-typescript
          --no-nodejs-compat
      - name: Verify executable set and evidence boundary
        run: node scripts/verify-deployment.mjs --generated-dir "$RUNNER_TEMP/univ-deploy-generated"
      - name: Lint
        run: npm run lint
      - name: Build deployable site
        run: npm run build
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
      - name: Audit complete dependency tree
        run: npm audit --audit-level=high
